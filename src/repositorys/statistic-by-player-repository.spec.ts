import { StatisticByPlayerRepository } from './statistic-by-player-repository';

describe('StatisticByPlayerRepository', () => {
  it('should be defined', () => {
    expect(new StatisticByPlayerRepository()).toBeDefined();
  });
});
